# Site configuration for development
# Override properties in _config.yml, if jekyll serve --config "_config.yml,_config_dev.yml" used.

# Server

# Overridden by --host <value> of jekyll serve command line
host: "localhost"
# Overridden by --port <value> of jekyll serve command line
port: 4000
# Overridden by --livereload of jekyll serve command line
livereload: false

url: "http://localhost:4000"

# Build

sass:
  style: "full"

# Plugins

plugins:
  # Need to repeat plugins for prod here
  - "jekyll-sitemap"
  - "jekyll-seo-tag"
  - "jekyll-github-metadata"
  - "jekyll-redirect-from"
  - "jekyll-paginate"
  - "jemoji"
  # Only dev
  - "jekyll-admin"
